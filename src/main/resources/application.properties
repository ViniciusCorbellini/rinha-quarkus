# --- GRAALVM ---
quarkus.native.additional-build-args=-H:+StaticExecutableWithDynamicLibC

# --- Aplicação ---
quarkus.application.name=rinha-quarkus
quarkus.http.port=${PORT:8080}
app.name=${HOSTNAME:app-default}
app.port=${PORT:8080}

# --- Banco de Dados ---
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=postgres
quarkus.datasource.password=postgres
quarkus.datasource.jdbc.url=jdbc:postgresql://postgres:5432/postgres_api_db

# --- Configuração do Pool de Conexões (Adicionado) ---
# O Hibernate usará este pool
quarkus.datasource.jdbc.min-size=5
quarkus.datasource.jdbc.max-size=10

# --- Hibernate ORM ---
# Equivalente ao ddl-auto=update
quarkus.hibernate-orm.database.generation=update

# Mostrar SQL (opcional, igual ao show-sql=true)
# quarkus.hibernate-orm.log.sql=true

# Dialeto (opcional — o Quarkus detecta automaticamente, mas pode deixar explícito)
quarkus.hibernate-orm.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Métricas e Log (opcional) ---
# quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n
quarkus.log.file.enable=false
quarkus.devservices.enabled=false